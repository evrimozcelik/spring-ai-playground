Run the app and test Spring AI Tools and Vector Database with the responses to the user queries

curl -X POST http://localhost:8080/evrim/assistant -H "Content-Type: text/plain" -d "How many customers are there?"